"use client";

/**
 * Legacy purple rail sidebar — unificado en UserSidebar dentro de /usuario/layout.
 * Se mantiene el archivo vacío export para no romper imports residuales.
 */
export function ProfessionalSidebar() {
  return null;
}
